name: 'mbr_snowflake_dbt_ai'
version: '1.0.0'
config-version: 2

profile: 'mbr_snowflake_dbt_ai'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]

models:
  mbr_snowflake_dbt_ai:
    bronze:
      +materialized: table
      +database: DEMO
      +schema: BRONZE
    silver:
      +materialized: table
      +database: DEMO
      +schema: SILVER
    gold:
      +materialized: view
      +database: DEMO
      +schema: GOLD_DIMENSIONAL
